# full-cycle-sistema-monolitico

## Passos

- clonar o projeto via `git clone https://github.com/BryanAislanZinn/full-cycle-sistema-monolitico.git`
- build do projeto via `npm install`
- rodar os testes do projeto via `npm test`